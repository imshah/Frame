/*app.directive('appDirective', appDirective);

function appDirective(){
	return {
		restrict: "AE",
		//replace: true,
		template: '<a data-toggle="pill" href="#{{tab.name}}">{{tab.name|uppercase}}</a>',
		scope :{
			href : '@',
			dataToggle: '@'
		},
		controller: "AppController",
		link: function(scope, elem, attrs, controller){
			scope.$watch(elem, function(newval, oldval){
				
			});

		}
	}
}*/